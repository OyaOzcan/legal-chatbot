# from query_neo4j import get_clauses_by_type
# from utils import personalize_clause

# def generate_contract(contract_type, party_a, party_b):
#     clauses = get_clauses_by_type(contract_type)
#     personalized_clauses = [personalize_clause(c, party_a, party_b) for c in clauses]
#     return "\n\n".join(personalized_clauses)
